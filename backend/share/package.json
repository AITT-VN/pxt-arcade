{
  "name": "gamelab-share-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Dịch vụ chia sẻ dự án cho GameLab (OhStem MakeCode Arcade)",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "sam build",
    "deploy": "sam deploy",
    "deploy:guided": "sam deploy --guided --profile ohstem",
    "local": "sam local start-api --profile ohstem"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.620.0",
    "@aws-sdk/client-s3": "^3.620.0",
    "@aws-sdk/lib-dynamodb": "^3.620.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  }
}
